name: Update Playlist Cookies

on:
  schedule:
    - cron: '0 */6 * * *'    # every 6 hours
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-playlist:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Extract Cookie and Update app.m3u for TiviMate
        env:
          GIST_ID: ${{ vars.GIST_ID }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          python << 'EOF'
          import requests
          import re
          import os
          import sys

          # ───────────────────────────────────────────────
          #   CONFIG
          # ───────────────────────────────────────────────
          WORKER_URL    = "https://zstar.pfy.workers.dev"
          M3U_FILENAME  = "app.m3u"
          GIST_ID       = os.getenv("GIST_ID")
          GH_TOKEN      = os.getenv("GH_TOKEN")

          if not GIST_ID or not GH_TOKEN:
              print("❌ Error: GIST_ID or GH_TOKEN is missing.")
              sys.exit(1)

          HEADERS = {
              "Authorization": f"token {GH_TOKEN}",
              "Accept": "application/vnd.github.v3+json"
          }

          # 1. Get fresh cookie from worker
          try:
              resp = requests.get(WORKER_URL, timeout=15)
              resp.raise_for_status()

              match = re.search(r'__hdnea__=([^"\'\s&]+)', resp.text)
              if not match:
                  print("❌ Could not find __hdnea__ in worker
